# MG-SeedDeleterMod
MG-SeedDeleterMod is a browser userscript (Tampermonkey/Greasemonkey) written in TypeScript that adds a bulk seed-deletion tool to a browser-based gardening game. It injects a small floating, draggable UI — a toggle button that expands into a control panel — letting the player select one or more seed
